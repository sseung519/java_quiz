package ch04;

public class Quiz01 {
    public static void main(String[] args) {
        /*
        1. 10<x && x<20
        2. ch != ' ' || ch != '\t'
        3. ch == 'x' || ch == 'X'
        4. '0' <= ch || ch <= '9'
        5. 'a' <= ch && ch <= 'z' || 'A' <= ch && ch <= 'Z'
        6. year % 400 == 0 || year % 4 == 0 && year % 100 != 0
        7. powerOn != true
        8. str.equals("yes")
         */
    }
}
